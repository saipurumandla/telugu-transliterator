# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# pytest
.pytest_cache/
.coverage
.coverage.*

# Data files — keep .gitkeep, ignore everything else
data/raw/*
!data/raw/.gitkeep
data/interim/*
!data/interim/.gitkeep
data/processed/*
!data/processed/.gitkeep
data/processed_v3/

# Generated reports — intermediate only; committed reports are tracked explicitly
reports/**/*.json
reports/**/*.md
!reports/benchmarks/baseline_report.md
!reports/benchmarks/baseline_eval.json
!reports/benchmarks/confusion_report.json
!reports/benchmarks/v3_benchmark_report.md
!reports/benchmarks/eval_results.json

# Model checkpoints — too large for git
train/checkpoints/
train/checkpoints_v3/
train/checkpoints_reverse/

# Generated manifest files from data pulls
data/manifests/*.json
data/manifests/snapshot_index.json

# Virtual environments
venv/
.venv/

# Distribution
dist/
build/
*.egg-info/

# Large dataset archives — never commit these
downloads/

# Env and logs
.env
*.log
*.jsonl
